-- Date operation, display today, yesterday and tomorrow date
select getdate() as Today, getdate()-1 as Yesterday, getdate()+1 as Tomorrow

-- Select specific columns with null value (Color)
select *
from SalesLT.Product p
where p.color is null

-- Select specific columns only (Name and ListPrice)
select p.Name, p.ListPrice
from SalesLT.Product p 
