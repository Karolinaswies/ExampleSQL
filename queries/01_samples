-- Date operation
  select getdate() as Today, getdate()-1 as Yesterday, getdate()+1 as Tomorrow

-- Select null
select *
from SalesLT.Product p
where p.color is null

-- Select only Name and ListPrice
select p.Name, p.ListPrice
from SalesLT.Product p 
